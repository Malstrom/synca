<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p><%= I18n.t("guest_mailer.magic_link.body", email: @user.email) %></p>
    <p><%= link_to I18n.t("guest_mailer.magic_link.link_text"), "#{deep_link_base_url}/activate?token=#{@token}" %></p>
  </body>
</html>