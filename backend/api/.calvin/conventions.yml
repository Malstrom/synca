naming:
  files: snake_case
  methods: snake_case
  classes: CamelCase
  constants: SCREAMING_SNAKE_CASE
  database_columns: snake_case
  boolean_columns: "suffix _verified or _at (e.g. spark_verified, deleted_at)"

folder_structure:
  controllers: app/controllers/api/v1/
  services: app/services/
  serializers: app/serializers/
  contracts: app/contracts/
  jobs: app/jobs/
  channels: app/channels/
  models: app/models/
  tests: test/

# ─── MIGRATIONS ──────────────────────────────────────────────────────────────────────
migrations:
  rule: |
    Generate a migration if and only if the issue contains a "## DB Changes" section.
    When that section is present, a migration is MANDATORY — do not skip it.
    When that section is absent, do NOT generate any migration.

  triggers:
    - "## DB Changes" section in the issue body

  required_when:
    - adding a new column to an existing table
    - adding a new table
    - adding or changing an index
    - adding an enum column to a model (enum in model + migration are always a pair)

  forbidden_when:
    - issue has no "## DB Changes" section
    - issue only touches controllers, services, contracts, serializers, or tests

  enum_rule: |
    NEVER add an enum to a model without its migration.
    The model enum declaration and the migration that adds the column are always generated together.
    A model with enum :field that has no corresponding migration will crash at boot.

  filename_rule: |
    Migration filenames MUST use a real numeric timestamp in the format YYYYMMDDHHmmSS.
    Use the current UTC time when generating the file.
    CORRECT:   db/migrate/20260702154200_add_account_type_to_users.rb
    WRONG:     db/migrate/[timestamp]_add_account_type_to_users.rb
    WRONG:     db/migrate/TIMESTAMP_add_account_type_to_users.rb
    "[timestamp]" is a placeholder — it is NOT a valid filename and Rails will ignore the migration.

  format: |
    class AddExampleColumnToUsers < ActiveRecord::Migration[8.0]
      def change
        add_column :users, :example, :integer, null: false, default: 0
        add_index  :users, :example
      end
    end

# ─── TESTING MANDATE ─────────────────────────────────────────────────────────────────
testing_mandate: |
  EVERY file of production code you create or modify MUST have a corresponding test file
  in the same commit. No exceptions.

  Mapping rules:
    app/controllers/api/v1/auth/guest_registrations_controller.rb
      → test/controllers/api/v1/auth/guest_registrations_controller_test.rb

    app/services/example_service.rb
      → test/services/example_service_test.rb

    app/models/user.rb (if modified)
      → test/models/user_test.rb (add new test cases — do not delete existing ones)

    app/contracts/example_contract.rb
      → test/contracts/example_contract_test.rb

    app/serializers/example_serializer.rb
      → test/serializers/example_serializer_test.rb

  What to test per layer is defined in testing.yml → always_test.
  Use fixtures from testing.yml → fixture_catalogue. Never invent new fixture names.
  Test class inherits from ApiTestCase for controllers, ActiveSupport::TestCase for everything else.
  Never require 'minitest/mock' — see testing.yml → forbidden.

  If you create a controller, you MUST test:
    - the happy path (correct status + response shape)
    - 401 on missing Authorization header (unless skip_before_action is used — then omit 401 tests)
    - 422 on missing or invalid params

# ─── API RESPONSE ───────────────────────────────────────────────────────────────────
api_response:
  concern: app/controllers/concerns/api_response.rb
  helpers:
    render_success:           "render_success(data, status: :ok)       → 200 + raw data hash"
    render_created:           "render_created(data)                    → 201 + raw data hash"
    render_error:             "render_error(code:, message:, field:)   → 422 by default; field is optional"
    render_unauthorized:      "render_unauthorized                     → 401 {error: {code: 'unauthorized', message: ...}}"
    render_not_found:         "render_not_found('SparkSession')        → 404 {error: {code: 'not_found', message: 'SparkSession not found'}}"
    render_validation_errors: "render_validation_errors(record)        → 422 using record.errors.first"
    render_contract_errors:   "render_contract_errors(result)          → 422 using dry-validation result.errors"
    auth_response:            "auth_response(user)                     → {access_token:, refresh_token:, user: {id:, email:}}"

  json_shapes: |
    # Success:
    { "spark_session": { "id": 1, "status": "pending" } }

    # Error:
    { "error": { "code": "validation_failed", "message": "Email can't be blank", "field": "email" } }
    # field key is omitted when nil — do NOT include it as null

    # Unauthorized:
    { "error": { "code": "unauthorized", "message": "Invalid or missing token" } }

    # Auth response:
    { "access_token": "...", "refresh_token": "...", "user": { "id": 1, "email": "a@b.com" } }

  rules:
    - success payloads use the resource name as key, never a generic {data:} wrapper
    - error codes are snake_case strings (validation_failed, not_found, unauthorized)
    - field: in errors identifies the offending parameter name as a string
    - never call render json: directly — always go through ApiResponse helpers

# ─── CONTROLLERS ───────────────────────────────────────────────────────────────────
controllers:
  rules:
    - all under Api::V1, mounted at /api/v1/
    - authentication via before_action :authenticate_user! in ApplicationController (inherited)
    - no inline response hashes — use Alba serializer
    - input validation via dry-validation contract
    - public methods map to standard REST actions only (index, show, create, update, destroy)
    - business logic delegated to service objects — never inline in controllers
    - include Dry::Monads[:result] and use case/in pattern matching on service results

  migration_note: |
    Some existing controllers (me_controller.rb, spark_sessions_controller.rb) still use
    params.require/permit and inline hashes — they predate the Alba + dry-validation decision.
    NEW controllers MUST use the contract + serializer + service pattern.
    Do NOT copy the old params.require/permit pattern.

  canonical_pattern: |
    # frozen_string_literal: true

    module Api
      module V1
        class ExamplesController < ApplicationController
          include Dry::Monads[:result]

          def create
            result = ExampleContract.new.call(params.to_unsafe_h)
            return render_contract_errors(result) if result.failure?

            case ExampleService.call(current_user: current_user, attrs: result.to_h[:example])
            in Success(record)
              render_created({ example: ExampleSerializer.new(record).serialize })
            in Failure[:validation_failed, message]
              render_error(code: "validation_failed", message: message)
            in Failure[:not_found, message]
              render_not_found(message)
            end
          end
        end
      end
    end

# ─── SERIALIZERS ───────────────────────────────────────────────────────────────────
serializers:
  location: app/serializers/
  naming: ResourceSerializer (e.g. UserSerializer, MatchSerializer)
  gem: alba (no pinned version in Gemfile)
  usage: "render_success ExampleSerializer.new(resource).serialize"

  canonical_pattern: |
    # frozen_string_literal: true

    class ExampleSerializer
      include Alba::Resource

      attributes :id, :status, :created_at

      attribute :partner do |resource|
        resource.partner ? UserSerializer.new(resource.partner).serialize : nil
      end
    end

  rules:
    - one serializer per resource, not per endpoint
    - nested associations use dedicated serializers
    - nil associations return nil, never raise
    - no inline hashes in controllers
    - only generate a serializer if the controller actually instantiates it

# ─── CONTRACTS ───────────────────────────────────────────────────────────────────
contracts:
  location: app/contracts/
  naming: OperationContract (e.g. CreateSparkSessionContract, RegisterUserContract)

  canonical_pattern: |
    # frozen_string_literal: true

    class HealthSummaryContract < Dry::Validation::Contract
      params do
        required(:health_summary).hash do
          required(:effective_from).filled(:date)
          optional(:chronotype).maybe(:string)
        end
      end

      VALID_CHRONOTYPES = %w[morning intermediate evening].freeze

      rule(health_summary: :chronotype) do
        next unless value
        key.failure("must be morning, intermediate or evening") unless
          VALID_CHRONOTYPES.include?(value)
      end
    end

  usage: |
    result = HealthSummaryContract.new.call(params.to_unsafe_h)
    return render_contract_errors(result) if result.failure?
    # result.to_h contains typed, validated attributes — pass directly to service

  rules:
    - params block always nests fields under the resource key (health_summary:, spark_session:, etc.)
    - date fields declared as :date — dry-types handles string→date coercion automatically
    - enum validations belong in rule blocks with a constant, not inline strings
    - params.require/permit is NOT used in controllers that have a contract

# ─── SERVICE OBJECTS ────────────────────────────────────────────────────────────────
service_objects:
  gem: dry-monads
  entry_point: "ServiceName.call(args) — always .call, never .new.call from outside"
  result_pattern: |
    Success(value)                        — happy path, value is the primary record/result
    Failure([:error_code, message])       — business failure, error_code is snake_case string
    include Dry::Monads[:result] in the service class
  controller_pattern: |
    include Dry::Monads[:result] in the controller
    case ServiceName.call(...)
    in Success(record)                    then render_success / render_created
    in Failure[:validation_failed, msg]   then render_error(code: "validation_failed", message: msg)
    in Failure[:not_found, msg]           then render_not_found(msg)
    end
  pure_services_note: |
    Services that never fail on business logic (e.g. CompatibilityService, RewardEngine)
    keep plain Data.define return values — do NOT wrap in Success/Failure.
    Reserve Success/Failure for services that have meaningful failure paths.
  location: app/services/

  canonical_pattern: |
    # frozen_string_literal: true

    class ExampleService
      include Dry::Monads[:result]

      def self.call(...) = new.call(...)

      def call(current_user:, attrs:)
        record = current_user.examples.build(attrs)

        if record.save
          Success(record)
        else
          Failure([:validation_failed, record.errors.full_messages.first])
        end
      end
    end

  existing_services:
    UpdateHealthSummaryService:
      file: app/services/update_health_summary_service.rb
      signature: "UpdateHealthSummaryService.call(current_user:, attrs:)"
      returns: "Success(health_summary) | Failure([:validation_failed, message])"

    CompatibilityService:
      file: app/services/compatibility_service.rb
      signature: "CompatibilityService.call(first_health_summary, second_health_summary)"
      result: "Data.define(:total, :sleep, :activity, :lifestyle, :preferences)"
      constants: WEIGHT_SLEEP, WEIGHT_ACTIVITY, WEIGHT_LIFESTYLE, WEIGHT_PREFERENCES
      note: Pure calculation service — returns Data.define, not Success/Failure. NEVER call directly from a job — always go through MatchScoringFacade

    JwtService:
      file: app/services/jwt_service.rb
      signatures:
        - "JwtService.access_token(user)  → String"
        - "JwtService.refresh_token(user) → String"
        - "JwtService.decode(token)       → { user_id: Integer } or raises JWT::DecodeError"
      note: called in ApplicationController#authenticate_user! and auth controllers

    RewardEngine:
      file: app/services/reward_engine.rb
      note: Pure side-effect service — issues SparkReward records post SparkSession completion. Returns nil, not Success/Failure.

    MatchScoringFacade:
      status: NOT YET IMPLEMENTED (V2 — do not generate stubs or calls to it)
      intended_signature: "MatchScoringFacade.call(match) → Result"
      note: |
        Referenced in conventions as the required entrypoint for background jobs.
        When implemented, jobs must call this, never CompatibilityService directly.
        ML_SCORING_ENABLED env var guards whether the ML path is active (default: false).

    MlEventLogger:
      status: NOT YET IMPLEMENTED (V2 — do not generate stubs or calls to it)
      intended_signature: "MlEventLogger.call(event_type:, user_id:, payload:) → nil"
      intended_behavior: |
        Must never raise — wraps all logic in rescue StandardError.
        Call at every interaction point in hot paths (match scoring, spark completion).
        When implemented, stub in tests via define_singleton_method on the instance.

  rules:
    - always include Dry::Monads[:result] in service class
    - services never call render — always return Success/Failure
    - pure calculation or side-effect services may return plain Data.define or nil
    - never call CompatibilityService directly from a job — go through MatchScoringFacade when it exists
    - do NOT generate calls to MatchScoringFacade or MlEventLogger until they exist in app/services/

# ─── MODELS ───────────────────────────────────────────────────────────────────
models:
  rules:
    - all inherit from ApplicationRecord
    - all files have # frozen_string_literal: true as first line
    - all FK columns point to users.id — NEVER profiles.id
    - soft delete: NOT YET IN DB (see schema.yml implementation_gaps). When added: deleted_at column + scope
    - enums use modern syntax only: enum :field, { value: integer }
    - before_save callbacks handle normalization (e.g. normalize_email → downcase)
    - custom validations use validate :method_name

  auth_providers: |
    User.auth_provider enum has FOUR values (not three):
      email: 0, apple: 1, google: 2, telegram: 3
    The schema shows auth_provider default: 0. telegram (3) is defined in the model
    but has no production flow yet — do NOT generate telegram auth controllers.

  soft_delete_pattern: |
    # Not active yet. When implementing:
    # Migration:
    add_column :examples, :deleted_at, :datetime
    add_index  :examples, :deleted_at
    # Model:
    scope :active, -> { where(deleted_at: nil) }
    def soft_delete! = update!(deleted_at: Time.current)

# ─── PATTERNS ───────────────────────────────────────────────────────────────────────
patterns:
  - all endpoints under /api/v1/ with Bearer JWT auth
  - background jobs: inherit ApplicationJob < ActiveJob::Base, enqueued via SolidQueue
  - external services stubbed in tests via define_singleton_method on the instance

pagination_defaults:
  default_page: 1
  default_per_page: 20
  max_per_page: 100
  gem: pagy
  response_shape: |
    {
      "matches": [...],
      "meta": { "total_count": 42 }
    }
  usage: |
    pagy, records = pagy(scope, items: params.fetch(:per_page, 20).to_i.clamp(1, 100))
    render_success({ resource_name => serialize(records), meta: { total_count: pagy.count } })

# ─── TESTING ─────────────────────────────────────────────────────────────────────────
testing:
  monad_assertions: |
    # Use assert_pattern for monad result matching in Minitest:
    assert_pattern { result => Success }                                # any Success
    assert_pattern { result => Success[health_summary] }               # Success with binding
    assert_pattern { result => Failure[:validation_failed, _] }        # specific failure code
    # Do NOT use result.success? / result.failure? booleans in tests

# ─── DO NOT TOUCH ───────────────────────────────────────────────────────────────────────
do_not_touch:
  score_thresholds:
    what: Numeric threshold values for match scoring and circle eligibility
    where: "docs/features/matching-v1.md and docs/features/circles-v1.md only"
    why: Thresholds are calibrated against real data; changing them breaks ML baseline.

  profiles_id_as_fk:
    what: profiles.id must NEVER be used as a FK target
    why: "User is the identity root. Profile is a dependent child that can be nil. See decisions.yml: fk_to_users."

  ml_event_logger_rescue:
    what: The rescue block inside MlEventLogger (when implemented) must never raise
    why: MlEventLogger is called in hot paths. An exception here would break the entire request.
    current_status: MlEventLogger does not exist yet — do not generate it unless the issue targets V2.

  ml_scoring_enabled_flag:
    what: ML_SCORING_ENABLED defaults to false in all environments
    why: MlMatchScoringJob is V2, not active in V1. Enabling it prematurely breaks scoring pipeline.

style_guide: docs/conventions/backend.md
